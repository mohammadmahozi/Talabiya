CREATE TABLE UserEntity(
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    name TEXT NOT NULL
);

selectAll:
SELECT id, name
FROM UserEntity
ORDER BY name;

insert:
INSERT INTO UserEntity(name)
VALUES (?);

delete:
DELETE FROM UserEntity WHERE id = ?;