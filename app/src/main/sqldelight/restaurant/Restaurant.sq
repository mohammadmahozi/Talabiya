CREATE TABLE RestaurantEntity(
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    name TEXT NOT NULL
);

insert:
INSERT INTO RestaurantEntity(id, name)
VALUES (?, ?);

selectAll:
SELECT id, name FROM RestaurantEntity ORDER BY name;

delete:
DELETE FROM RestaurantEntity WHERE id = ?;
